[package]
name = "burn_raymarching"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.20", features = ["wgpu", "autodiff"] }
serde = { version = "1.0", features = ["derive"] }
image = "0.25"
